buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.moowork.gradle:gradle-grunt-plugin:0.5'
    }
}

apply plugin: 'grunt'

// Doese not work yet...
task gruntXXX(type: GruntTask) {
    dependsOn ':com.toedter.chatty.server:runServer'
}